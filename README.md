# Postfix-Expression-Solver-C
The program reads a postfix expression containing constants and unknown variables (e.g., a, b, c) from an input.txt file. The core challenge is that these unknowns can be either positive integers or arithmetic operators ($+, -, *, /, ^$).
